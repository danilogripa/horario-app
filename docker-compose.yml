services:
  horario-app:
    build: .
    container_name: horario-app
    ports:
      - "7500:7500"
    volumes:
      - horario-data:/app/data
    environment:
      - DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  horario-data:
